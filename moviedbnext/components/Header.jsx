import UserInfo from './UserInfo';
export default function Header() {
    return (
        <header>
            <div className="app-title">MovieDB App</div>
            <UserInfo />
        </header>
    );
}
